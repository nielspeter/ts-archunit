# ts-archunit Roadmap

**Version:** 0.58.0 · **Tests:** 3195 across 235 files, plus 42 in `tests/matrix/` (post-build) · **Updated:** 2026-08-07
**Spec:** `../ts-archunit-spec.md` · **Direction:** `ai-era-product-direction.md`
**Plans:** 78 completed (`completed/`) · 14 open (files in `plans/`) · proposals in `../proposals/` ·
open defects and their priority order in [`../bugs/BUGS.md`](../bugs/BUGS.md)

> Conventions: a plan is **READY** when its design is settled and it can be built as
> written; **PROPOSED** when the design is reviewed but nobody has decided to build
> it; **PARTIAL** when some phases shipped and the rest were deliberately deferred.

---

## The fail-closed programme (ADR-009)

Four plans, in dependency order. Each is one PR that leaves `main` green; a _release_ bundles
several, so ADR-009's "one red event" holds while the plans stay individually completable.

| Plan                                                                                                                    | State                                                                                                                                                                                                                       | Blocked on                                                                        |
| ----------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| [0095](./completed/0095-the-vacuity-matrix-and-the-conformance-audit.md) — the vacuity matrix and the conformance audit | **DONE 2026-08-07.** Measured the whole published surface for the first time: 4 families fail open, 1 preset constructs zero rules, 1 fails closed by its own instrument, the rest guarded. The truth table is in bug 0066. | done                                                                              |
| [0096](./0096-evidence-at-every-seam.md) — evidence at every seam, and the preview                                      | **OPEN, high.** Five families count their examined units; `zero-subjects` lands in `diagnose()` so test-runner users get a preview at all. Additive; ships first.                                                           | 0095 — done                                                                       |
| [0097](./completed/0097-the-declared-empty-grammar.md) — the declared-empty grammar                                     | **OPEN, high and cheap.** Hoists `expectEmpty`/`expectNonEmpty` to `TerminalBuilder` (also ADR-010 rule 3(a)) and converts `correspondence().allowEmpty()`.                                                                 | nothing                                                                           |
| [0098](./0098-the-evidence-seam-and-the-floor.md) — the evidence seam and the floor                                     | **OPEN, high.** Retypes `collectViolations()`, adds the floor, carries bug 0066's fix. The one red event.                                                                                                                   | 0096, 0097, [0089](./0089-presets-forward-their-options.md), **ADR-010 ratified** |

[ADR-009](../adr/009-a-pass-is-constructed-from-evidence.md) is **Proposed** and goes to Accepted on
0095's measurement — which is why 0095 ran first: an ADR ratified on two measured cells is an argument,
and the table is what makes it a measurement.

---

## Open work

| Plan                                                                                                                              | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Effort       | Blocked on                              |
| --------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | --------------------------------------- |
| [0076](./completed/0076-resolve-module-edges-once-per-file.md) — Build module edges once per file                                 | **DONE, shipped 0.31.0.** `edgesOf()` rebuilt a file's edges per call: five whole-project `notImportFrom` rules made **10,545 `getSymbol` calls in 46ms** where one pass would do. Caches the **built edges**, not resolution — measured, of 48ms across five passes the walk is 1ms, resolution 15ms and construction ~32ms, so this file's own "cache resolution, not the walk" docstring was aimed at the middle third. The dangerous part is invalidation: a `SourceFile`'s identity **survives an edit**, so 0075's key argument does not transfer — `onModified` (ts-morph, ADR-002 intact) invalidates per file. `edgeStream` reads the cache warm and never fills it cold, preserving `dependOn`'s early exit. 6/6 sabotages caught.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | ~half a day  | done                                    |
| [0075](./completed/0075-collect-elements-once-per-project.md) — Collect elements once per project                                 | **DONE, shipped 0.31.0.** `filterElements()` re-collects from scratch on every rule execution. Measured on this repo (518 files): **5 × `calls()` = 2,590 descendant queries / 684ms where 518 / 131ms would do** — 5.2x waste that scales with rule count. A prototyped memo gets it to 518 / 144ms (**4.75x**) with all 31,560 subjects identical elementwise. Also covers `agentGuardrails` (8 banned APIs: 142 → 83ms), which is the commoner case because it is a preset we ship. No behaviour change. Four measured constraints, the load-bearing one being **memoize `getElements()`, never `filterElements()`** — a `filterElements()` memo makes `within(sel).functions()` share a key with `functions()` and pass vacuously.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | ~1 day       | nothing                                 |
| ~~[0077](./completed/0077-doctor-promote-it.md) — `doctor`: promote it~~                                                          | **DONE, and reversed during its own investigation.** Draft 1 said retire, on the ground that `doctor` cannot load the shape the docs lead with. Measured: it cannot load a vitest-importing file (true), but `getting-started.md:10` says **the CLI is the default path** and `init` scaffolds a rule file with zero vitest imports — so `doctor` works on the documented default. What earns it a slot is a **dead selector glob**: `check` never calls `diagnose()`, so it exits **0 with no output** on a rule that can never match while `doctor` exits 1 and names the site. A second false claim — that only `doctor` catches load failures — was written into the plan and caught by review: `check` catches those too, exits 1, and prints a remedy. Promoted to `--help` with its scope stated; `--format` validated. **6 of 6 sabotages**, two of which first scored MISSED against a revert string that matched nothing. Still unblocks [0074](./completed/0074-r3b-the-selector-glob-flip.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | ~2 hours     | a decision                              |
| ~~[0074](./completed/0074-r3b-the-selector-glob-flip.md) — R3b: the selector glob flip~~                                          | **DONE, shipped v0.34.0 — breaking.** Gate run 4 (2026-07-31) ran the pre-flight on both sides. This repository: 36 rules collected as values, **zero findings** — refuted as the gate codebase for the fourth time, and the population is 36, not the 43 recorded here and in the plan. `honojs/hono` with the published 0.32.0 and the unedited `init --preset layered` scaffold: **10 findings**, and **6** with the tsconfig repointed at one that loads. Against the decision rule — does any message assert a cause wrong for that input? — **yes, twice**: [0031](../bugs/fixed/0031-diagnose-blames-the-glob-when-the-project-loaded-nothing.md) and [0032](../bugs/fixed/0032-an-absent-path-defers-to-a-cause-list-it-refutes.md). **Both shipped in v0.33.0**, so the messages R3b converts into build failures have been corrected and verified against the codebase that found them. Three pieces: a dead **selector** glob fails at check time; an **empty selection** fails, with `.expectEmpty()` as the escape hatch and `.notExist()` exempt because zero subjects is what it asserts; and a preset fan-out collapses to **one finding per option** (measured at 83 from one bad `shared` glob). Implementation added the cardinality exemption to the selector row, which 0069 had specified for `emptyIsPass` only — 5 of the glob gate's first 9 failures. Two of the appendix's four constraints were violated and caught by reading further. Sabotage 8/8, 8/8, 10/10.                                                                                                                                                                                                                                                                                                                                                                        | ~1 day       | done                                    |
| ~~[0079](./completed/0079-triage-the-cardinality-only-assertions.md) — Triage the cardinality-only assertions~~                   | **DONE 2026-08-03, v0.45.4.** Sampled 30 by seed and classified every one by reading: **27%** were class C — a count standing in for identity, passing on a swap — against a ~15% stop rule, so Phase 2 ran. All **45** of 143 converted. Six of the eight sampled Cs carried a **comment naming the identity the assertion omitted**. The scan itself was a hand-maintained list and missed five element-boolean idioms; refined, then validated against the hand classification (5 of 5 false positives dropped, 8 of 8 confirmed Cs kept). Two blocks were reclassified to A by measurement — their violations are identical by design, so the count IS the value. Swap proof: the old count assertions PASS on a swapped fixture and the new identity assertions FAIL. Found a library gap on the way ([0082](./completed/0082-an-object-literal-callback-keeps-its-name.md)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | —            | done                                    |
| ~~[0084](./completed/0084-cycle-detection-that-ignores-type-only-imports.md) — Cycle detection that can be turned on~~            | **DONE 2026-08-04, shipped 0.47.0.** `beFreeOfCycles(options?)` now defaults to `ignoreTypeImports: true` and `arch/no-cycles` runs at `.check()` for the first time. Turning it on found two cycles: `helpers → models`, **one day old** — plan 0082's fix added the value edge and nothing failed because nothing could — fixed by moving `object-literal-functions.ts` to `core/`; and the pre-existing `[builders, conditions, helpers, predicates]`, waived by identity as [bug 0054](../bugs/fixed/0054-within-makes-helpers-depend-on-builders.md), which turned out to be **already waived from the other direction** since plan 0015. Inventory row 3 was **wrong and the implementation right** ("a runtime cycle in one direction" is not a thing); row 4 rested on a false premise and became [0085](./completed/0085-the-slice-graph-cannot-see-a-re-export.md). **8 sabotages, 2 caught by nothing:** reverting `.check()` to `.warn()` was unguarded — the plan removed the instance and left the mechanism — now fixed by narrowing `gate()` to return `{ check }`; the second became [0086](./0086-a-remedy-that-names-the-folder-it-selects.md). The first matrix reported 8/8 CAUGHT and **every verdict was false**, three separate ways.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Small-medium | done                                    |
| ~~[0085](./completed/0085-the-slice-graph-cannot-see-a-re-export.md) — The slice graph cannot see a re-export~~                   | **DONE 2026-08-04, shipped 0.48.0.** All three slice conditions now read `edgesOf()` — the definition plan 0071 wrote and this call site never adopted, which is why `export { x } from './banned.js'` was a dependency to `notImportFrom` and invisible to `beFreeOfCycles` **in the same run**. `import`/`reexport` count; `dynamic` (lazy, and usually the deliberate cycle fix), `require` (CJS) and `type-expression` (erased, excluded by _kind_ so the option cannot add it) do not. `findSliceDependencyDetails` fixed too — the plan's unmeasured claim held: without it the two layering conditions find every re-export dependency and report **nothing**. `notDependOn`/`respectLayerOrder` gained `ImportOptions` with the _opposite_ default to cycles, documented beside both. **Row 8 was vacuous for us** — our three in-slice re-exports are all intra-slice and same-slice edges are skipped, so `arch/no-cycles` staying green proved nothing; the real proof is `src/index.ts`, 90 re-exports and zero plain imports. **12 of 12 sabotages, after 5 were caught by nothing** — two edge-kind exclusions argued only in a docstring, and a row that asserted the same answer for both values of the option it claimed to test. The harness lied twice more: `"CAUGHT BY NOTHING".startswith("CAUGHT")` printed "0 of 12" under a visible gap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Small-medium | done                                    |
| ~~[0087](./completed/0087-an-inline-type-import-still-requests-the-module.md) — An inline type import still requests the module~~ | **DONE 2026-08-04, shipped 0.49.0.** `ModuleEdge.typeOnly` answered two questions at once. Measured through ts-morph's emit: under `verbatimModuleSyntax`, `import { type X } from 's'` emits `import {} from 's'` and `export { type X } from 's'` emits `export {} from 's'` — specifiers erased, **module request kept** — so both can close a real cycle and `beFreeOfCycles` reported nothing. Exactly **two** of eight forms are flag-dependent, and both are the "every specifier inline-typed" branch, which collapsed the fix to one formula. New `erasesModuleRequest` beside an unchanged `typeOnly`; the slice graph takes a **required** `ErasureQuestion` because no default is right for both callers. Surfaced something the plan missed: `notDependOn`/`respectLayerOrder` sit on the cycle graph but ask the coupling question, so one edge is correctly a cycle _and_ correctly not a type-import dependency. Two on-disk fixtures, byte-identical source, opposite flags. **14 of 14 sabotages** after two rounds; both initial gaps were decisions whose only witness was a comment. One probe nearly misled me — it showed `import { type X, y }` as erased, an artifact of the value binding being _unused_.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Medium       | done                                    |
| [0088](./0088-a-slice-finding-identifies-itself.md) — A slice finding identifies itself                                           | **OPEN, high — the keystone.** No slice condition sets `ArchViolation.identity`, so the hash falls back to `element::message`. That one fact causes three filed bugs: barrel findings **collapse to one baseline entry** (measured: 3 sites, 3 lines, 1 hash — bug 0028's shape in the family that never got the fix), the cycle message **cannot be improved** without invalidating every cycle baseline (blocking bug 0055), and the cycle identity **carries traversal order** (bug 0056). Phase 1 is the whole plan: decide what each finding _is_. Phase 3 spends the freedom — name the closing edge and its kind, which `edgeVerb()` already supports and no slice condition uses, and make `docs/slices.md`' per-edge listing true rather than aspirational.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Medium       | nothing                                 |
| [0089](./0089-presets-forward-their-options.md) — Presets forward their options                                                   | **OPEN, medium.** `layeredArchitecture` and `strictBoundaries` hardcode their conditions, so the population most exposed to three behaviour changes in one day has only a severity override. `layeredArchitecture` runs `respectLayerOrder` **and** `beFreeOfCycles` over one slice set and they now disagree by design about type edges — a preset user cannot align them. Also: every upgrade row scopes itself by an API name (`slices()`, `beFreeOfCycles()`) that a preset user never types, so both rows read as "not me". The durable half is a guard asserting each row names the presets containing the condition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Small-medium | nothing                                 |
| [0090](./0090-a-warn-that-expires.md) — A `.warn()` that expires                                                                  | **OPEN, medium-high.** Plan 0084's `gate()` trick solved for us a problem every adopter has, and `docs/upgrading.md` actively teaches the pattern (`.asSeverity('warn')` … "Then ratchet") with nothing to make the ratchet visible. ADR-008 rule 6 already names "a check with a scheduled expiry" as a blast-radius row — the concept is in the ADRs and absent from the API. Also missing: any way to distinguish a warning that is _advisory_ (rule 1 permits it) from one that is _deferred debt_ (rule 1 forbids it). Recommendation is a count ceiling plus reporting, not an expiry date. Phase 3 is the proof: delete `gate()` and express the same constraint through shipped API.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Medium       | nothing                                 |
| [0091](./0091-a-stub-marker-is-delimited-not-cased.md) — A stub marker is delimited, not cased                                    | **OPEN, medium.** Bug 0053 gave up case-insensitivity for every user in order to reject two _wrapped-JSDoc_ prose lines in this repository's unusually marker-dense docstrings. Cost: `// todo:` (the VS Code / ESLint default spelling) and `// NOT IMPLEMENTED` silently stop matching, on a rule shipping at `error` in the preset aimed at generated code. A **delimiter** rule — marker followed by `:`, `(` or end-of-line — rejects `stub,` and `deferred.` mid-sentence while keeping every casing. Row 1 of the inventory is the premise: if the delimiter rule does not reject bug 0053's prose table, close the plan.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Small-medium | after bug 0060                          |
| [0092](./0092-the-residual-guard-gaps-from-the-0-47-0-49-review.md) — The residual guard gaps from the v0.47–0.49 review          | **OPEN, low-medium.** The five review items triaged and deliberately NOT done in v0.49.2, recorded so the deferral is a decision. All are guards that work whose evidence is weaker than it could be — the sharpest is that `re-export-edges.test.ts`' independent derivation is sitting one row away and never compared, and the reviewer measured why naive line-set equality would false-red (prettier-wrapped multi-line `export … from`, where the graph reports the statement's start line and a line regex misses it). Compare specifier sets instead: text vs compiler resolution. Also records three things **verified sound** so nobody re-checks them, and three `erasesRequest` branches that are unreachable rather than uncaught — a distinction the new rule 5 corollary exists to make.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Small        | nothing                                 |
| [0093](./0093-a-reference-consumer-for-the-presets.md) — A reference consumer for the presets                                     | **OPEN, medium — and argue it before building it.** Split out of plan 0083 Phase 3 when that phase's two hard requirements shipped without it. What remains is the expensive third: every preset over one project with a per-rule-id finding map — which 0083's own text warns is "a snapshot in all but name, with a snapshot's churn and none of the `-u` escape hatch". The plan's first task is a table of candidate defects against what now exists, and only ONE row is uncovered (a rule that stops firing entirely) — which needs no reference project at all, just the declared-id set `orphanExclusions` already derives. **Consider closing it in favour of that.** Carries the five design constraints Phase 3 earned, so they are not rediscovered.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Medium-large | a justification                         |
| [0094](./0094-the-residual-findings-from-the-v0-56-0-review.md) — The residual findings from the v0.56.0 review                   | **OPEN, medium.** The five-persona review items triaged and deliberately NOT done in v0.56.0, recorded so the deferral is a decision. Two are live fail-opens in guards: the ordinal's **counter key** is unguarded (mutating `kind::specifier` to either half leaves the whole suite green, while the `specifier`-only form destroys the "identity survives edits above it" property the ordinal exists for), and `ignoreTypeImports` × the two newly-counted kinds has **no row at all** — a false positive on the escape hatch and a silent revert of this release's headline fix each leave the suite green. §3 must leave this plan and become its own bug: `edgeViolation`'s identity keys on the **resolved** path while every discriminator derives from the **specifier**, so a `paths` alias and a relative path collapse to one identity (2 findings, 1 hash) — pre-existing, module-family only, NOT a v0.56.0 regression and NOT fixed by keying the ordinal on the resolved path, all three of which were got wrong once during review. Also: `kindsFor` is a symbol that exists nowhere but is named in two places, `edgeStream`'s JSDoc still documents the cold/warm contract this release inverted, and the sort's stated rationale is measurably false (the counter key includes `kind`, so the case it cites cannot occur).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Small-medium | a bug number for §3                     |
| [0086](./0086-a-remedy-that-names-the-folder-it-selects.md) — A remedy that names the folder the rule selects                     | **OPEN, medium.** ADR-008 rule 2 demands remedies _verified to remediate_ and nothing enforces it. `arch/helpers-no-builders` shipped "Move the shared logic to src/helpers/" to a file **already in `src/helpers/`** — half a remedy that is provably a no-op for every violation the rule can produce. Fixed by hand in 0084; sabotage proved the class unguarded. The general problem is undecidable, but this slice is mechanical: a remedy must not name a location the rule's own selecting predicate matches. Starts as a test over our own 37 rules, per rule 6.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Small-medium | nothing                                 |
| [0083](./0083-eat-our-own-dogfood.md) — Eat our own dogfood                                                                       | **PARTIAL, and restructured 2026-08-04 after measurement.** Phase 3's two hard requirements shipped (v0.51.0); its wrapper split to [0093](./0093-a-reference-consumer-for-the-presets.md). **Phase 1 DONE and now RATCHETED.** I briefly claimed it stale — "36 measured, 44 now, eight unplanted" — and that was a miscount of `gate()` call sites: **37 distinct ids then and now**, seven rules gated in more than one place. The result table was right all along (36 planted, 1 deferred by name). It now carries a guard rather than a number: the unplanted set must equal the stated deferrals, derived from `BUILT`. **Phase 0 DONE** — `tests/tools/scan-enforceable-primitives.ts` derives the population from source and runs in CI: a function reachable from one of `package.json`'s twelve declared `exports` subpaths, returning `Condition`, `PairCondition` or `Predicate`. **181 of 274 public functions.** The version released as v0.55.3 said 150, reading only `src/index.ts` — a fifth wrong number, corrected the same day after two reviewers independently measured **31 primitives published only through the other eleven subpaths** (all of metrics, typescript, naming, dependencies, code-quality, graphql), three of which `arch-rules.test.ts` already applies. The 13.0% is withdrawn with its method: name matching undercounts (`.resideInFolder(...)` applies `conditionResideInFolder` 18 times without naming it) and overcounts (an import list scores as applied). No ratio ships. **Phase 2's priority lowered on evidence**: five reviewers found nine shipped-behaviour defects the census could not have surfaced. **New Phase 4** — do we enforce our own ADR? Audited: rules 3 and 4 have machinery, rule 2 has **none** while being cited in 21 test files, rule 6 is a convention on 17 of 88 plans with no test. | Per phase    | nothing                                 |
| [0072](./0072-a-denylist-glob-that-cannot-match.md) — A denylist glob that cannot match                                           | **REFUTED.** A denylist typo (`notImportFrom('**/legcay/**')`) is silent forever, and so is `onlyHaveTypeImportsFrom` — measured, 0 findings against 13 and 9 for the real glob. But BOTH proposed mechanisms died: a runtime exercise tally cannot tell a typo from a respected ban (tested>0, matched==0 either way), and static satisfiability cannot either — `**/legacy/**` in a repo with no `legacy/` is a legitimate pre-emptive ban, and `docs/modules.md:38` teaches that exact glob. 0069's own table had it right before this plan re-opened it. What survives is an author-declared opt-in, unbuilt and not yet justified.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | —            | a product decision, not a design gap    |
| ~~[0073](./completed/0073-conditions-declare-their-globs.md) — Conditions declare their globs~~                                   | **DONE 2026-07-30.** The population was **12**, not the seven the plan wrote: parsing found `structural.ts`'s `resideInFile`/`resideInFolder` (the generic element twins, publicly exported and used by three builders) plus three delegating aliases in `rules/`. Behaviour-neutral — 2588 tests passed unchanged — so the guard asserts the declaration itself: population derived from source two ways that must agree, kind per condition, tree followed into `globs()`. **21 of 21 sabotages caught.** The plan's `explain` guard was dropped as vacuous: `describeRule()` already interpolates the glob into the description, so it prints the forbidden path with the whole change reverted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | —            | done                                    |
| 0067 — Empty-selector safety (**part C only**)                                                                                    | **PARTIAL**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | ~1 day       | a version decision (breaking re-cut)    |
| 0047 — TypeScript escape-hatch matchers                                                                                           | **PROPOSED**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | ~1 day       | go/no-go — trimmed scope already agreed |
| 0048 — `usingTagged()` symbol-tagged matcher                                                                                      | **PROPOSED**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | ~1.5 day     | go/no-go — deferred until demand        |

All three need a decision, not implementation. 0063 shipped 2026-07-25.

**0067 part C** is the remaining slice of proposal 014: path-glob auto-fail on every
builder plus path normalization. It is the broadest breaking change left, which is
why it is parked on a version decision rather than an implementation question.

**0047 / 0048** were reviewed 2026-07-13 and given a go/no-go on 2026-07-14: 0047
ship **trimmed** (`doubleCast` + `anyAnnotation`; defer `broadType`/`tsDirective`),
0048 **defer until demand** (overlaps `@typescript-eslint/no-deprecated`; the symbol
layer is speculative). Both remain PROPOSED — the open question is whether each earns
API surface, not how to build it.

## Open defects

Measured and reproduced. Full write-ups in `../bugs/`; fixed ones move to
`../bugs/fixed/`, so this table lists only what is still open.

A `**Fixed:**` date is the convention going forward, not a property of the
directory: 6 of the 16 files in `bugs/fixed/` carry one (0001-0009 predate it).
Nothing enforces it, which by this project's own standard makes it a stated
invariant without a guard — so the location, not the header, is what this table
relies on.

| Bug                                                                                                                                                               | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ~~[0038](../bugs/fixed/0038-a-typo-in-a-preset-override-key-is-a-silent-false-green.md)~~ — a typo in a preset override key is a silent false green               | **FIXED, v0.41.0 — both halves.** The override key is now a literal union **derived** from each preset's rule ids, so a typo is a compile error before any run; `recommended` derives it from `SPECS` via `as const satisfies`, so no second list can drift. `agentGuardrails` is the honest exception — its `no-inline-logic/${api}` ids are open by construction, and the runtime finding covers that arm. That finding is a **sibling** of `validateOverrides`, not a change to it, because the published `void` signature is documented. 8 sabotages across two oracles, 8 caught — and the first type matrix read `head`'s exit code instead of `tsc`'s, scoring every row green.                                                                                                                                                                                                                                                                                                                                                      |
| ~~[0039](../bugs/fixed/0039-an-undocumented-exclusion-comment-suppresses-and-only-warns.md)~~ — an undocumented exclusion comment suppresses and only warns       | **FIXED, v0.38.0 — both halves.** A reason-free directive is now an unsuppressable configuration finding; the exemption still applies, so adding a reason clears it and keeps the exclusion working — a remedy that trades one failure for another is not a remedy. And nested blocks nest: block state became a stack, where it refused any nested `-start` and then let the inner `-end` close the outer, breaking both regions at once. The ceiling is unchanged and stated on the finding itself — `: needed` reaches green in one step, so this buys a reason in the diff for a human reviewer, not prevention. 5 of 6 sabotages; the sixth found `severity: 'error'` was a dead field (`bypassFilters` already forces it) and it was removed.                                                                                                                                                                                                                                                                                         |
| ~~[0040](../bugs/fixed/0040-a-crosslayer-rule-reports-nothing-when-its-layer-resolves-nothing.md)~~ — cross-layer reports nothing on an empty layer               | **FIXED — API half v0.42.0, silence half v0.44.0** via [plan 0080](./completed/0080-admit-discovery-globs-to-the-dead-glob-gate.md). `haveMatchingCounterpart` needed a `Layer[]` no public API could produce; the builder now supplies its own through a `PairConditionContext`. And two of three conditions reported **nothing** on an empty layer (4 violations → 0) — all three share one check now, and a dead `discovery` glob fails at every entry point, because `diagnose()` and the gate had used inverse hand-maintained position lists that disagreed about exactly `discovery`. The plan's own fix for the ownership question was unbuildable and was replaced: slice produces nothing for a partial empty _deliberately_, so "prefer what the builder produced" reads silence as no-opinion. Builders declare ownership instead. 7 of 7 sabotages.                                                                                                                                                                            |
| ~~[0043](../bugs/fixed/0043-an-exclusion-directive-inside-a-string-literal-suppresses.md)~~ — an exclusion directive inside a string literal suppresses           | **FIXED, v0.40.0.** The directive text inside `"…"`, `'…'`, a template, a regex or JSX text produced a live exclusion that silenced a real finding — silently, since a directive with a reason never warns. Literals are parsed and blanked now. A `ts.createScanner` first attempt fixed three cases and left two: a bare scanner has no parser context for template middles or JSX children. Fixing it exposed a second fault — a comment _about_ the syntax became the syntax, and the first casualty was the parser's own grammar documentation, caught by this repo's preset fan-out test. A directive must now begin its comment. 6 of 8 sabotages; the two green ones corrected an overclaim in the fix's own docstring.                                                                                                                                                                                                                                                                                                             |
| ~~[0044](../bugs/fixed/0044-an-inline-exclusion-comment-has-no-feedback-channel.md)~~ — an inline exclusion comment has no feedback channel                       | **FIXED, v0.43.0.** `doctor` reports a directive naming a rule id nothing declares — the rename case, the one that is silent _forever_. It cannot be a check-time finding without parsing every file in scope on every run, which is the cost the existing gate avoids (rule 1's migration corollary: a command beats a warning). Deliberately not part of `diagnose()`, because the declared-id set is the union across rule files and a per-file check would report siblings as orphans — pinned as a footgun test. 5 of 5 sabotages; the fifth found `doctor` could stop calling it with everything still green, the same wiring gap the v0.37.0 disclosure had.                                                                                                                                                                                                                                                                                                                                                                         |
| ~~[0045](../bugs/fixed/0045-two-tests-fail-by-environment-and-corrupt-sabotage-verdicts.md)~~ — environment failures corrupt sabotage verdicts                    | **FIXED, v0.39.1.** Filed as two flaky tests; one was a **shipped defect**. `Dirent.isDirectory()` is false for a symlink, so the prune check inside that branch never saw a symlinked `node_modules` — it was recorded as a file, and globs beneath it classified `absent` ("no such path", a claim) instead of `not-determined` ("this walk cannot say"). pnpm and `git worktree` both produce that layout, so real users got the wrong advice. Prune now happens by name, before the directory test. The second half — a shared `tests/__generated__` that one process's `beforeAll` deleted out from under another — is per-process now, proven by forcing the race deterministically after three concurrent attempts failed to reproduce a ~1-in-10 flake. Third cause recorded in ADR-008 rule 5: two agents sabotaged one checkout simultaneously and each credited the other's failures. 3 of 4 sabotages, the fourth green because the race needs two processes and a suite run is one — pinned structurally and labelled as such. |
| ~~[0041](../bugs/fixed/0041-an-exclusion-comment-is-a-no-op-for-most-conditions.md)~~ — an exclusion comment is a no-op for most conditions                       | **FIXED, v0.37.0.** A documented public feature that silently did nothing for the dependency, exports, slice, reverse-dependency and module-body families: `applyFilters` stamped `ruleId` _after_ the comment filter that bails without one. Enrichment now runs first. Its only end-to-end test used a helper that stamped the field itself — same-derivation, agreeing while the feature did not work. 4 of 5 sabotages, the fifth accounted for as unreachable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ~~[0042](../bugs/fixed/0042-cross-layers-empty-layer-finding-inherits-the-authors-remedy.md)~~ — cross-layer's empty-layer finding inherits the author's remedy   | **FIXED, v0.37.0.** Bug 0021 live at a producer its fix never reached, because this one _assigned_ the fields rather than leaving them to be withheld. Fixing it produced **two more wrong remedies** — an impossible clause (`RangeError` on a two-layer chain) and correct advice pointing at the wrong file (widening the builder glob does nothing; the condition reads the caller's array). Neither was catchable by asserting message content; only applying the fix found them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ~~[0046](../bugs/fixed/0046-cross-document-links-rot-silently.md)~~ — cross-document links rot silently                                                           | **FIXED, unreleased.** **82** broken links, not the 39 first reported — the original scan was narrower. 78 repaired mechanically, one by hand (0072 cited bug 0014 under a name it no longer has). The check handles **two dialects**: relative paths in `adr/`/`bugs/`/`plans/`/`proposals/`, and VitePress root-absolute routes in `docs/` — where a filesystem rule reports all 159 links broken and the correct rule reports zero, so the user-facing surface got checked instead of scoped out. The checker needed its own fix first: 82 findings, **78 real**, the four false positives all links inside code, two of them in documents about link syntax. 4 detector rows including a control that a fenced example stays green. Anchors deliberately unvalidated — two slug algorithms, 2 anchored links in the corpus.                                                                                                                                                                                                             |
| ~~[0047](../bugs/fixed/0047-a-fileless-finding-renders-a-meaningless-location.md)~~ — a fileless finding renders a meaningless location                           | **FIXED, v0.39.0.** Plain emitted a bare `(:1)`; JSON emitted `"file": "", "line": 1` and carried **no field at all** distinguishing a configuration finding from a real one. Both fixed, plus `kind: "violation"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | "configuration"`— a string rather than the boolean first shipped, because the third kind already exists and one flag was flattening four distinct remedies.`ArchJsonReport`is now exported and pinned against the emitter. Review caught a release blocker in the docs: the agent page said`file`is null and told the reader not to open it, but`attributeToRuleFile`rewrites it to the **rule file** first — so an agent detecting these by a null file would misclassify every attributed one. 6 of 6 sabotages, two of them green until a probe aimed at`formatViolations(v, 'plain')`— whose second parameter is`reason` — was pointed at the plain formatter. |
| ~~[bug 0048](../bugs/fixed/0048-the-dead-glob-gate-blames-the-glob-when-the-project-is-empty.md)~~ — the dead-glob gate blames the glob when the project is empty | **FIXED, v0.42.1.** (Note: distinct from _plan_ 0048 above — plans and bugs number independently.) On an empty project the gate told the reader to correct a **correct** glob, on the path every `modules()`/`classes()`/`functions()`/`types()` rule takes. Three implementations of "the project loaded nothing" existed — `diagnose.ts`, `SliceRuleBuilder`, and none in the gate — and the parity test compared the two that had one, because it drives a _discovery_-position glob the gate skips. `loadedNothing()` now owns the decision; three became one. 5 of 5 sabotages: the fifth found `bypassFilters: false` left the subset green, which is worse than untested, since the message claims it cannot be suppressed.                                                                                                                                                                                                                                                                                                          |
| ~~[0031](../bugs/fixed/0031-diagnose-blames-the-glob-when-the-project-loaded-nothing.md)~~ — `diagnose()` blames the glob when the project loaded nothing         | **FIXED, v0.33.0.** Reported once per project naming the tsconfig, not once per glob. Review then found the fix's own message wrong three ways — "Reported once" was false (`doctor` calls `diagnose()` per rule file), the remedy was impossible where no sibling tsconfig exists, and the dedup keyed on a path `workspace()` makes non-unique, silently dropping the loser. The empty-project text now has one owner shared with the failing-`check` message, pinned by a parity test. 11 of 11.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ~~[0033](../bugs/fixed/0033-assignedFrom-does-not-accept-a-project-relative-glob.md)~~ — `assignedFrom()` rejects a project-relative glob                         | **FIXED, v0.36.1.** The one surface 0067 part C left behind, so `layers: { api: 'src/api/**' }` failed beside a `shared: ['src/shared/**']` that worked in the same preset call. Root taken from the `ArchProject`'s own tsconfig path rather than ts-morph's `configFilePath`, which is undefined for an in-memory project. Both declaration sites moved too — the `base`, so `doctor` stops calling a working rule dead, and the fault classification, so a missing folder is not told to add an anchor. `matching()` deliberately still uses its looser rewrite; aligning it narrows matches and needs its own release. 6 of 6.                                                                                                                                                                                                                                                                                                                                                                                                          |
| ~~[0035](../bugs/fixed/0035-a-workspace-has-no-single-root.md)~~ — a workspace has no single root                                                                 | **FIXED, unreleased.** `workspace()` sets `tsConfigPath` to the alphabetically FIRST config, and 0067 C + 0033 promoted that tie-breaker into semantics: a relative glob meant one package, silently and green, and adding a package named `aaa` would re-point every rule. Bug 0011's class. Every root is now registered and each file resolves against the one containing it. Two more defects fixed with it — `''` meaning both "no root" and "root is `/`", and `isProjectRelative` disagreeing with `isAnchored` on `C:/`. The fix introduced a dependency cycle that **this repo's own `beFreeOfCycles` rule caught**.                                                                                                                                                                                                                                                                                                                                                                                                               |
| ~~[0037](../bugs/fixed/0037-an-import-glob-rejects-the-relative-spelling.md)~~ — an import glob rejected the relative spelling                                    | **FIXED, v0.36.2.** A **false red**: `layeredArchitecture({ shared: ['src/shared/**'], strict: true })` reported a violation on a correct architecture, because `shared` also reaches `onlyImportFrom(...)`, matched against the ABSOLUTE resolved path. No config finding and a silent `doctor` — condition-position globs are exempt by design and `import-target` has no path views (bug 0014). The root-relative form is now appended as one more candidate: appended so baselined identities do not move, and bare specifiers keep their early return. 5 of 5.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ~~[0036](../bugs/fixed/0036-the-relative-glob-audit-is-incomplete.md)~~ — the relative-glob audit is incomplete                                                   | **FIXED, v0.36.3.** The census is now **derived from source**: a new path-glob entry point fails the suite until classified, which the hand-written table could never do. It found **four** unclassified surfaces, three of them broken — `importFrom` selected 0 where anchored selected 5, and `onlyBeImportedVia` was a **false red** (5 violations vs 0). The census also caught two defects in itself: a line-based scan that missed a call prettier had split, and a file that names the kinds without declaring any. Honest residue recorded: `crossLayer`'s runtime half is unobservable through the public API — a pair rule reports nothing whether its layer resolves three files or none, which is 0067-D's shape at an entry point R3b never covered.                                                                                                                                                                                                                                                                          |
| ~~[0034](../bugs/fixed/0034-comment-matcher-underreports-and-goes-silent-on-re-evaluation.md)~~ — the `comment()` matcher under-reports                           | **FIXED, v0.36.0.** Found by the architect review of [0047](./0047-typescript-escape-hatch-matchers.md), which was going to build on this machinery. Three defects, one cause — a comment is not the node it is attached to: the first match in each scope was swallowed, the reported line was the node's, and a dedup Set that was never reset made a re-evaluated rule return nothing. Measured, `noStubComments()` found **5 of 9** stub comments. The first fix shipped a **regression** — stacked comments collapsed, a ratchet hole in the agent stub detector — and its own guard certified it; three reviewers caught it. 11 of 11 sabotages, derived from the diff.                                                                                                                                                                                                                                                                                                                                                               |
| ~~[0032](../bugs/fixed/0032-an-absent-path-defers-to-a-cause-list-it-refutes.md)~~ — an `absent` path defers to a cause list it refutes                           | **FIXED, v0.33.0.** `absent` states the fact and the causes it leaves standing. The first fix shipped a NEW confidently-wrong message twice over: it asserted "nothing matching this exists on disk" (measured false — a `(marketing)` directory picomatch reads as a group, and a sibling package outside the identity root), and it carried plan 0072's pre-emptive-ban excuse into `selector` position, where a glob matching nothing IS the false green 0069 is named after. Its guard also reimplemented the selection: two mutations of the real code left 2719 tests green.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ~~[0015](../bugs/fixed/0015-allowlist-conditions-pass-vacuously-on-edgeless-subjects.md)~~ — allowlist conditions pass vacuously on edgeless subjects             | **FIXED (unreleased).** Option 2 — reported, not failed. Failing was refuted on measurement: for the `only*` family zero edges is **maximal compliance**, and every remedy (add an import, exclude the rule, narrow the selector, delete it) makes something worse. A rule with subjects and zero tested edges is now named in `--format json`'s `untestedAllowlists` and in a stderr footnote. 8/8 sabotages caught — after three gaps the first matrix found, two of them tests that could not fail.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ~~[0012](../bugs/fixed/0012-metric-findings-have-no-usable-ratchet.md)~~ — improving a metric turns the build red                                                 | **FIXED (unreleased).** The per-element threshold ratchet this bug specified: a metric finding gets a stable identity (`Big::methods`, no value) plus `measured`, the baseline stores the accepted value, and `isKnown` compares rather than equates. Paying down debt is green; regressing past the accepted value is red. One mechanism for all eight sites. Two diagnostics had to learn the difference — a regressed metric is neither a full replacement nor a renamed rule. `HASH_VERSION` 3 → 4, metric entries only. 8/8 sabotages caught.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| [0019](../bugs/fixed/0019-a-rule-with-no-condition-passes-in-total-silence.md) — a rule with no condition passes silently                                         | **FIXED in v0.23.0** via [plan 0070](./completed/0070-a-rule-must-assert-something.md). 0.22.0 completed the instrument — `doctor`/`diagnose()` report every such rule with the remedy for its own shape; 0.23.0 made it an unsuppressable configuration finding on every terminal, which closes this.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| [0020](../bugs/fixed/0020-should-twice-silently-drops-the-first-assertion.md) — `should()` twice drops the first assertion                                        | **FIXED in v0.23.0** via [plan 0070](./completed/0070-a-rule-must-assert-something.md): conditions accumulate instead of clearing, with a `HASH_VERSION` bump so a stale baseline reports the right cause.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| [0017](../bugs/fixed/0017-boundaries-no-cross-boundary-message-overclaims-entry-point-enforcement.md) — `no-cross-boundary`'s remedy cannot remediate             | **FIXED in v0.25.0.** The `no-cross-boundary` remedy said "import from the other boundary's entry point instead", which reproduces the identical violation — measured. Three strings corrected, the `suggestion` computed because no fixed text is right when `shared` is empty. Baseline-free (an old-text baseline replays with 0 new findings), which also corrected a docstring claiming otherwise. `explain --format agent` no longer repeats one bullet per boundary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| [0022](../bugs/fixed/0022-forward-import-conditions-are-blind-to-reexports-and-dynamic-imports.md) — forward import conditions miss two edge kinds                | **FIXED in v0.28.0** via [plan 0071](./completed/0071-one-definition-of-a-module-edge.md). One definition of a module edge: 662 import declarations → 835 edges over `src/` (+26%), `src/index.ts` 0 → 114. Blinding the two main conditions now fails 110 tests across 25 files, up from 38 across 12 — the release added coverage, not just visibility.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| [0023](../bugs/fixed/0023-strictboundaries-shared-globs-are-raw-and-unguarded.md) — `shared` globs raw and unguarded                                              | **FIXED in v0.25.0.** A `shared` glob matching no file is now a configuration finding, the treatment `folders` has had since plan 0067 — the relative and dead spellings were indistinguishable from outside (same violation count, same silence). The report's normalization half was **withdrawn**: `atPath` is about file-vs-folder, not relative-vs-absolute, and `folders` is not normalized either, so rewriting `shared` globs would have made one option accept a spelling the other rejects.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ~~[0028](../bugs/fixed/0028-two-findings-in-one-file-can-share-a-baseline-identity.md)~~ — two findings can share a baseline identity                             | **FIXED in v0.29.0.** Dependency findings carry a distinct `identity` from the edge's imported names and line — measured, **8 colliding pairs of 47 (17%)** in this repo. The fix was **not** migration-free, and this file's own "option 2 needs no migration" claim was wrong: setting `identity` replaces the whole hash subject, so **every** dependency entry changes hash, not only the colliding ones. `HASH_VERSION` 2 → 3, and a non-empty baseline matching nothing now emits a diagnostic — the difference between a silent invalidation and a stated one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ~~[0030](../bugs/fixed/0030-user-defined-predicates-and-conditions-cannot-declare-globs.md)~~ — user-defined predicates and conditions cannot declare globs       | **FIXED in v0.30.0.** `definePredicate` / `defineCondition` take an optional `globs` third argument. Verified through the real CLI: a custom predicate's dead glob now makes `doctor` print the site and **exit 1**, where before it exited 0. The plumbing needed no change and that was measured first — a hand-built predicate literal with globs already reached `globs()` and was already reported. It also caught a **vacuous assertion in 0073's own guard**: that test used an `import-target` glob, which has no path universe, so it stayed green with the condition-position skip removed. Now covered with `onlyBeImportedVia`, which is genuinely checkable.                                                                                                                                                                                                                                                                                                                                                                   |
| [0029](../bugs/fixed/0029-a-throwing-warn-truncates-the-rest-of-the-rule-file.md) — a throwing `.warn()` truncates the rest of the rule file                      | **FIXED.** The CLI reports the truncation — the half [plan 0069](./completed/0069-no-rule-may-certify-nothing.md) R3a specified and shipped without — and each finding is reported once. Scoped to a thrown terminal: for a syntax error nothing ran, so the notice would imply otherwise. Guarded against real rule files on disk, with the `export default [...]` shape as the control.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| [0024](../bugs/fixed/0024-warn-terminal-is-invisible-inside-a-test-runner.md) — `.warn()` in a test reports nothing                                               | **FIXED in v0.26.0.** One `writeStderr` channel, ten call sites across five files — the scope was wider than the report: the stale-exclusion, `expression()`, diff-aware and invalid-baseline warnings were all invisible in a passing test too. Guarded by a real child `vitest run` in both directions, because a spy proves the call and never the delivery. Also closed a live EPIPE defect in `writeReport`. 57 tests moved off `console.warn` spies.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| [0025](../bugs/fixed/0025-a-non-archruleerror-from-one-rule-file-drops-every-other-finding.md) — one bad rule file drops every finding in the run                 | **FIXED in v0.24.0.** Both halves: the CLI now catches at the two boundaries that fail independently (loading per file, evaluating per builder, so one malformed rule does not take its siblings down), and `CorrespondenceBuilder.assertsSomething()` returns false for wrong arity regardless of the assertion chosen — `.beComplete()` on one side cannot assert anything. Fixing it surfaced three output defects, all fixed: the rich formatter never printed a located violation's `message`, `--format github` doubled a period, and the finding named its path four times.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| [0026](../bugs/fixed/0026-a-location-less-finding-does-not-say-which-rule-file-it-came-from.md) — a location-less finding is not locatable                        | **FIXED in v0.24.0.** `attributeToRuleFile` stamps the rule file (line 1, the `tsconfig()` precedent) onto findings with no location of their own, at the point in the CLI loop where the mapping exists; `doctor` now diagnoses per file and carries `ruleFile` on each finding. Surfaced that the `ts-archunit-exclude` immunity for configuration findings had no test — and this change is what makes it live — plus two source comments naming a directive that does not exist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| [0027](../bugs/fixed/0027-an-unmatched-baseline-entry-cannot-be-diagnosed.md) — an unmatched baseline entry cannot be diagnosed                                   | **FIXED in v0.24.0**, and not by the mechanism the report proposed: comparing the entry's `rule` string cannot detect a _description_ change, because the description is what changed. A `subject` hash over `element::message` separates "the rule was edited" from "the violation was fixed"; the specific diagnosis supersedes the generic `matched === 0` finding because a matched subject disproves its repository-root explanation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

### Fixed

| Bug                                                                                                                                   | Landed                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [0010](../bugs/fixed/0010-violation-identity-embeds-absolute-paths.md) — identity embeds abs paths                                    | v0.19.0, after a review round found two criticals in the fix itself.                                                                                                                                                                                                                                         |
| [0011](../bugs/fixed/0011-dogfood-rules-select-nothing.md) — dogfood rules select nothing (17, not the 14 filed)                      | v0.20.0. Fixed by rescoping the rules, not by the bespoke mechanism it originally proposed — see below.                                                                                                                                                                                                      |
| [0013](../bugs/fixed/0013-resolvers-cannot-see-resolvers.md) — collectors blind to object literals                                    | v0.19.0. `resolvers()`, both smells and two presets could not see handler-map functions.                                                                                                                                                                                                                     |
| [0014](../bugs/fixed/0014-bare-package-import-globs-match-nothing.md) — bare package globs match nothing                              | v0.20.0. `notImportFrom('fastify')` compared the glob to the _resolved_ path, so it matched only when the package was NOT installed.                                                                                                                                                                         |
| [0016](../bugs/fixed/0016-narrowing-a-named-selection-mutates-it.md) — a held selection was mutated                                   | v0.21.0. 40 chain methods across 12 classes returned `this` after mutating it, so a held selection lost subjects and later rules passed. Nine of those classes were outside `RuleBuilder`'s hierarchy, and the GraphQL pair forked in neither `that()` nor `should()` — the shape `docs/graphql.md` teaches. |
| [0018](../bugs/fixed/0018-data-layer-preset-silently-enforces-nothing-for-a-file-glob.md) — a preset enforced nothing for a file glob | v0.20.0. `repositories`/`shared`/layer globs went to `resideInFolder`, which reads the parent directory, so a file glob matched nothing.                                                                                                                                                                     |

**0011 was fixed by rescoping, not by a mechanism** — kept here for the 0067-C
rationale, which still stands. It originally proposed a file-set identity
assertion. The 0067-C measurement superseded that: running the empty-selector
flip from a checkout **not** named `ts-archunit` produced 23 failures against 10
from a correctly-named one, and the 13-failure delta was exactly these rules.
That is the argument for fixing the class rather than the instances, and it is
why R3b still subsumes this even though the rules themselves were rescoped in
v0.20.0. An earlier version of this paragraph ended "0011 is now waiting on the
0067-C decision" while the table above recorded it as shipped — the same
table-versus-prose contradiction the Open/Fixed split was written to remove.

**Proposal [019](../proposals/019-rules-that-enforce-nothing-must-fail.md) got
cheaper.** It replaces `console.warn(...) + return []` at four sites — a rule
that has subjects but no conditions asserts nothing and passes. **The four sites
are deleted by [plan 0070](./completed/0070-a-rule-must-assert-something.md)'s 0.22.0 work** —
`assertionAdvice()` replaced them with one per-state remedy read by `doctor`, closing
[bug 0019](../bugs/fixed/0019-a-rule-with-no-condition-passes-in-total-silence.md)'s
observation that the main one could not fire (it was gated on
`_phase === 'predicate'`, which `should()` had already left). But 0014 merged the two builder hierarchies into one root, so the
fix is now a single implementation on `TerminalBuilder` instead of five copies,
and it composes with the census's `conditions: 0`, which already reports the
state without failing on it.

019 and 0067-C are the two halves of one guard: **019 is empty conditions,
0067-C is empty subjects.** Neither is a defect report; both are decisions.

## Deferred — decided, not scheduled

- **Discovery surface → adoptable** — [proposal 018](../proposals/018-adoptable-discovery-surface.md),
  **Draft 3, parked on bug 0010.** The strategic gap is real and unchanged: the
  enforcement half is mature, the discovery half (`duplicateBodies`,
  `inconsistentSiblings`) is used essentially zero times, and pointing it at a large
  adopting codebase surfaced ~700 findings that all 177 of that project's enforced
  rules were blind to. Review killed the obvious diagnosis — severity was never the
  blocker (`.check()` already works on smells via `overrides`), and a count ratchet is
  forbidden by ADR-008 rule 5. What remains is bug 0010 plus one parked question:
  whether a working baseline is enough to adopt a 700-finding surface, or a violation
  budget is needed.
- **`runtimeIsolation`** — proposal unwritten.
- **Slice discovery guards** — failing on a single-slice result, and on an empty slice
  among populated siblings. Both catch real false-greens; both were built and withdrawn
  from 0.18.1 because they fire on legitimate projects with no opt-out. They return once
  each remedy is executable data and an opt-out exists (`correspondence().allowEmpty()`
  is the model). See `../bugs/fixed/0009-*.md`.
- **015 (Bun)** — decided: no core preset. A tier-3 `@ts-archunit/bun` package, gated on
  a second real app. Nothing to build in core.
- **Proposal 012 (workspace config-coverage)** — rejected as core (2026-07-17): the root
  fix is a package-manager flag and the problem is structurally absent on
  pnpm/Turborepo/Nx. One idea extracted for its own proposal: reasoned exclusions
  (`deferred(pattern, reason)` beside `silent()`).

---

## Releases

| Version    | Theme                                                                                                          |
| ---------- | -------------------------------------------------------------------------------------------------------------- |
| **0.22.0** | The assertion instrument — `doctor`/`diagnose()` see every rule that asserts nothing, each with its own remedy |
| **0.21.0** | A held builder is immutable — 40 chain methods across 12 classes are copy-on-write ⚠️ behaviour                |
| **0.20.0** | `doctor` / `diagnose()` and the glob declaration model; config findings cannot be downgraded                   |
| **0.19.0** | Portable violation identity — `withBaseline()` works across checkouts; three collectors see handler maps       |
| **0.18.1** | Slice glob parsing (every spelling agrees); meta-finding remedies visible and unsilenceable                    |
| **0.18.0** | AI-era program — `correspondence()`, object-literal functions, empty-selector safety ⚠️ breaking               |
| 0.17.0     | `init` scaffolds the shape presets                                                                             |
| 0.16.0     | Docs restructure (golden path); shape presets → returning form ⚠️ breaking                                     |
| 0.15.0     | `tsconfig()` config-assertion rule                                                                             |
| 0.14.0     | `ts-archunit init` scaffolder                                                                                  |
| 0.13.0     | AI-agent delivery program                                                                                      |

### 0.18.0 — the AI-era program (PR #2, 2026-07-24)

Turned the "architecture governance for the AI era" positioning into shipped code.
Positioning lives in `docs/why-ts-archunit.md`; the sequenced roadmap in
`ai-era-product-direction.md`. Motivated by an audit of a large adopting codebase —
its two largest recurring bug classes were route↔permission-matrix drift and phantom
limits, plus an empty-selector false-green the tool itself committed twice.

| Plan | What                                                 | Public API                                                                                                               |
| ---- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 0064 | F1 — filtered-subject materialization (the keystone) | `RuleBuilder.subjects()`                                                                                                 |
| 0065 | F2 + proposal 017 — coverage/relation primitive      | `correspondence(p).side()` → `.beComplete()` / `.haveNoOrphans()` / `.beBijective()`; `byName`/`byArg`/`byPropertyNames` |
| 0066 | F3 + proposal 016 — object-literal functions         | `functions(p, { includeObjectLiteralFunctions })`                                                                        |
| 0067 | F4 + proposal 014 (A/B/D) — empty-selector safety    | `.expectNonEmpty()`; `ArchViolation.bypassFilters`; slice/preset discovery non-vacuity                                   |

---

## Capabilities

What the library can do today. Details in `../docs/`.

**Entry points** — `modules()` imports and dependency direction · `classes()`
inheritance, decorators, members · `functions()` declarations, arrows, methods, and
optionally object-literal values · `types()` interfaces and aliases, matching through
`Partial`/`Pick` · `slices()` cycles (Tarjan), layer order, isolation · `calls()`
framework-agnostic call matching.

**Body analysis** (the differentiator) — `call()`, `newExpr()`, `access()`,
`property()`, `expression()`, `comment()` matchers with `contain()` / `notContain()` /
`useInsteadOf()`; optional-chaining normalization and nested-call detection.
`within(selection)` scopes rules inside matched callbacks.

**Relations & consistency** — `correspondence()` for "every X has a matching Y" by
identity · `crossLayer()` route↔schema↔SDK · `smells.duplicateBodies()` and
`smells.inconsistentSiblings()` AST-fingerprint detectors.

**Member inspection** — property, parameter-type, return-type and call-argument
conditions; visibility predicates.

**Adoption** — `withBaseline()` and `diffAware()` for existing codebases; per-rule
severity overrides; `.excluding()` / `silent()`; config-level findings deliberately
bypass both filters.

**Presets & standard rules** — `layeredArchitecture`, `dataLayerIsolation`,
`strictBoundaries`, `recommended`, `agentGuardrails` (all composable, returning form);
standard rules under `rules/` — `architecture`, `code-quality`, `dependencies`,
`errors`, `hygiene`, `metrics`, `metrics-function`, `naming`, `security`, `typescript`.

**CLI & output** — `check` (with `--watch`), `baseline`, `explain`, `init`;
terminal / JSON / GitHub-annotation formats; `tsconfig()` compiler-option assertions.

**Extension** — `definePredicate()`, `defineCondition()`, `.satisfy()`,
`definePattern()` / `followPattern()`, `not()` / `and()` / `or()` combinators.

---

## Completed plans

77 plans in `completed/`, numbered 0000–0087 (counted, not incremented). Accepted proposals that shipped without
a numbered plan live in `../proposals/completed/` (multi-project workspace, dynamic
imports, builtin `importsFrom`, silent exclusions, JSX element rules, call-argument
identity, per-rule exclusions).

The per-plan history lives in those files and in `../CHANGELOG.md`; it is deliberately
not duplicated here — three overlapping "what's done" tables were what made the
previous version of this file hard to read.
