// The corrected remedy's action, already applied: depend on the shared module
// rather than on the other boundary. Must produce no violation.
import { shared } from '../../shared/util.js'
export const c = shared
